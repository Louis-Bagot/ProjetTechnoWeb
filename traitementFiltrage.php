<?php
		if(isset ($_POST ["action"]) {

			if (empty ($_POST ["Destination"]) && empty ( $_POST["Logement"]) && empty ( $_POST ["texte"]) && empty ( $_POST ["Durée"])) {

			//rien n'est coché comme critère : on ne fait rien

			}

			else if (!empty ($_POST ["Destination"]) && !empty ( $_POST["Logement"]) && !empty ( $_POST ["texte"]) && !empty ( $_POST ["Durée"])) {

			//requete dans le cas où tous les champs sont remplis

			}

			else if (  ) {

			}



		}
		include("PageAccueil.php");
	?>
